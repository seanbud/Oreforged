import { Facet } from '../types';
/**
 * Takes a Facet and returns a Promise that will resolve with its first value
 *
 * @param facet
 * @returns Promise<T>
 */
export declare const asPromise: <T>(facet: Facet<T>) => {
    promise: Promise<T>;
    cancel: () => void;
};
