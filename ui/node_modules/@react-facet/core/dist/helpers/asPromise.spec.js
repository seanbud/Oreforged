"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const __1 = require("..");
const facet_1 = require("../facet");
const asPromise_1 = require("./asPromise");
it('immediately resolves facets with their values if available', () => __awaiter(void 0, void 0, void 0, function* () {
    const facet = (0, facet_1.createFacet)({ initialValue: 'testing' });
    const value = yield (0, asPromise_1.asPromise)(facet).promise;
    expect(value).toEqual('testing');
}));
it('waits for the facet to have a value, making sure to not hold subscriptions', () => __awaiter(void 0, void 0, void 0, function* () {
    const cleanupSubscription = jest.fn();
    const startSubscription = jest.fn().mockImplementation((update) => {
        update('testing');
        return cleanupSubscription;
    });
    const facet = (0, facet_1.createFacet)({
        initialValue: __1.NO_VALUE,
        startSubscription,
    });
    const value = yield (0, asPromise_1.asPromise)(facet).promise;
    expect(value).toEqual('testing');
    expect(startSubscription).toHaveBeenCalledTimes(1);
    expect(cleanupSubscription).toHaveBeenCalledTimes(1);
}));
//# sourceMappingURL=asPromise.spec.js.map