"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const startFacetTransition_1 = require("./startFacetTransition");
const scheduler_1 = require("./scheduler");
jest.mock('react', () => ({
    startTransition: jest.fn().mockImplementation((fn) => fn()),
}));
jest.mock('./scheduler', () => ({
    batchTransition: jest.fn(),
}));
it('acts as a startTransition, but wrapping the provided fn within a batchTransition', () => {
    const fn = jest.fn();
    (0, startFacetTransition_1.startFacetTransition)(fn);
    expect(scheduler_1.batchTransition).toHaveBeenCalledTimes(1);
    expect(scheduler_1.batchTransition).toHaveBeenCalledWith(fn);
});
//# sourceMappingURL=startFacetTransition.spec.js.map