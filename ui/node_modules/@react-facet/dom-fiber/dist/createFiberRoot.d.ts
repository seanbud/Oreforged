import { FacetFiberRoot, ReactFacetReconciler } from './types';
/**
 * @private consider using render instead
 */
export declare const createFiberRoot: (reconcilerInstance: ReactFacetReconciler) => (container: HTMLElement) => FacetFiberRoot;
